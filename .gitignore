# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies
node_modules

# Build output
dist
coverage
*.tsbuildinfo

# Local environment
.env
.env.local
.env.*.local
# azd state — the demo `dev` env is checked in so `azd init -t ... -e dev`
# starts with the right resource group / subscription. Any other env name
# stays local-only.
.azure/*
!.azure/dev/
.azure/dev/*.log

# Editor and OS files
.vscode/
.idea/
.DS_Store
